elementweb:
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      external-dns.alpha.kubernetes.io/target: "homelab-tunnel.kaleb.ashbyte.com"
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    hosts:
      - host: &frontend_host chat.kaleb.ashbyte.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: element-tls-certificate
        hosts:
          - *frontend_host
  config:
    default:
      base_url: https://matrix.kaleb.ashbyte.com
      server_name: kaleb.ashbyte.com

dendrite:
  dendrite_config:
    global:
      server_name: matrix.kaleb.ashbyte.com
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostName: matrix.kaleb.ashbyte.com
    tls:
      - hosts:
          - matrix.kaleb.ashbyte.com
        secretName: matrix-tls-certificate
  postgresql:
    enabled: true
